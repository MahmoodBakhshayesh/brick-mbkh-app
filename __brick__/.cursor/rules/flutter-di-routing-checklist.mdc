---
description: Mandatory DI and routing checklist for new or updated features
alwaysApply: true
---

# DI and Routing Checklist

When creating or changing a feature, ensure all of the following:

- Add route name constant in `core/navigation/app_route_names.dart`.
- Add route entry in `core/navigation/router_provider.dart`.
- For detail/edit routes, pass route IDs via provider overrides.
- Create `lib/features/<feature>/<feature>_di.dart` with `register<Feature>Dependencies(GetIt)`.
- Wire the registrar in `lib/core/di/app_features_di.dart` (manual features) or `lib/core/di/generated_features_di.dart` (`mbkh_feat` output).
- Use `registerFeatureStack` in the feature DI file:
  - `name` must end with `DataSource` (e.g. `ProfileDataSource` → `ProfileDataSourceRemote` / `ProfileDataSourceLocal`).
  - `createRepository: FeatureRepository.builder`
- Register each use case: `locator.registerLazySingleton(() => MyUsecase(locator()));`
- Add ARB keys to `lib/l10n/app_en.arb` and `lib/l10n/app_fa.arb` for user-visible strings.
- Do not leave a feature partially wired (files exist but route/DI missing).

See `WIRING_REQUIRED.md` for the full template.

# Completion Rule

- Before finishing, verify: navigation → controller → `runUseCase` → use case → repository → data source.
