# {{app_title}}

Generated with `mbkh_app` Mason brick.

## Included

- MBKH feature-first folder structure
- Core/helpers/interfaces/controllers/widgets baseline files
- Core navigation scaffold
- DI scaffold
- `.cursor/rules` starter rules
- Seed feature: `{{#snakeCase}}{{initial_feature_name}}{{/snakeCase}}`
- Initial features list: `{{initial_features}}`

## Next steps

1. Run `flutter create .` in this folder (if platforms are not yet present).
2. Run `flutter pub get`.
3. Run the app.
4. Use `mbkh_feat` brick for new features.
